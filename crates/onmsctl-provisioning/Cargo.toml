[package]
name = "onmsctl-provisioning"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Provisioning capability for onmsctl: requisition + foreign-source composite kind, declarative `apply -f`, XML→YAML migrator, import lifecycle"
publish = false

[dependencies]
onmsctl-core = { workspace = true }
clap = { workspace = true }
# Additional dependencies (serde, serde_norway, quick-xml, schemars,
# thiserror, async-trait, anyhow, percent-encoding, tokio) are added
# in the tasks that first consume them — see openspec change
# add-provisioning-capability tasks.md groups 2–8.
