[package]
name = "onmsctl-provisioning"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Provisioning capability for onmsctl: requisition + foreign-source composite kind, declarative `apply -f`, XML→YAML migrator, import lifecycle"
publish = false

[dependencies]
onmsctl-core = { workspace = true }
clap = { workspace = true }
serde = { workspace = true }
serde_norway = { workspace = true }
# Additional dependencies (quick-xml, schemars, thiserror, async-trait,
# anyhow, percent-encoding) are added in the tasks that first consume
# them — see openspec change add-provisioning-capability tasks.md
# groups 3 (schema), 4 (diff), 8 (convert).

[dev-dependencies]
serde_norway = { workspace = true }
