---
title: onmsctl
slug: /
description: onmsctl is a kubectl-style command-line interface for OpenNMS Horizon.
---

import VersionBadge from '@site/src/components/VersionBadge';

<VersionBadge />

A `kubectl`-style command-line interface for [OpenNMS Horizon](https://www.opennms.com/horizon/).
One declarative entrypoint, `onmsctl apply -f`, peeks each YAML document's `kind` and routes it to the right handler.
This lets users, event sources, SNMP config, requisitions, maintenance windows, and data-collection sources all reconcile through a single command.
XML→YAML migrators bring legacy eventconf and `provision.pl`-shape files into the loop.
Reads, explicit deletes, and `convert` stay imperative.

Start here: [Install](getting-started/install.md), then the [Quick Start](getting-started/quickstart.md).

:::caution Pre-stability notice
`v0.x.y` releases may break CLI flags, the config schema, and the `EventSource` YAML schema between minor versions.
Surfaces stabilize at `v1.0.0`.
:::
